The ci build takes too long if we have to pull all the packages for all the projects so we use this instead.

Adding a new type:
>> npm install @types/<typename>

- Find @types/<typename> in node_modules
- Move the the <typename> folder into here instead

